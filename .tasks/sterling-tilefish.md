---
id: sterling-tilefish
title: Mont lock needs better semantics
type: jot
---

When running mont lock without arguments, it should default to locking ALL gates for the active task.
Only if there is one active task.

When runnning like this, we should confirm with user.

Need to figure out the semantics better.
